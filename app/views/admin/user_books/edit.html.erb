<h1>Editing Admin User Book</h1>

<%= form_for([:admin, @user_book]) do |f| %>
    <% if @user_book.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@user_book.errors.count, "error") %> prohibited this user_book from being saved:</h2>

            <ul>
                <% @user_book.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <%= f.label :user_id, :class => "control-label" %><br>
        <%= f.text_field :user_id %>
    </div>

    <img src="<%= @user_book.url %>">
    <!--<div class="field">-->
        <%= f.label :url, :class => "control-label" %><br>
        <!--<%= f.text_field :url %>-->

    <!--</div>-->

    <div class="field">
        <%= f.label :book_id, :class => "control-label" %><br>
        <%= f.number_field :book_id %>

    </div>

    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>

<%= link_to 'Show', [:admin, @user_book] %> |
<%= link_to 'Back', admin_user_books_path %>
